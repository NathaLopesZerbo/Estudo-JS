
//Métodos são termos utilizados, para indicar uma função!


// .concat() serve para concatenar um array a outro
const numbers = [5, 3, 4]
const otherNumbers = [1, 2, 3]
const allNumbers = numbers.concat(otherNumbers);
console.log(allNumbers)

// toUpperCase() utiliza para colocar todo o texto = MAIÚSCULO

const text = "Algum texto"
console.log(text.toUpperCase())
console.log(typeof text.toUpperCase)

// indexOf() utiliza para saber a posição do que, quer ser encontrado
console.log(text.indexOf("g"))


