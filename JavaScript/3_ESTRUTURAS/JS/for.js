
    //12 - for

    //Serve para simplificar a lógica
    // 1° Colocamos o incrementador (variavel = 0)
    // 2° Condição final (variavel < 0)
    // 3° Progressão (variavel++ ou variavel--)

    for (let t = 10; t < 10; t++){
        console.log("Repetindo algo...");
    }

    let r = 0;

    for( r; r>0; r = r-1){
        console.log(`teste ${r}`)
    }